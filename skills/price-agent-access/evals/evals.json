{
  "skill_name": "price-agent-access",
  "evals": [
    {
      "id": 0,
      "prompt": "I want our agent node to charge 0.05 USDC per call on Polygon Amoy, paid to 0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266. USDC on Amoy is 0x41E94Eb019C0762f9Bfcf9Fb1E58725BfB0e7582 and it has 6 decimals. Give me the exact request.",
      "expected_output": "POST /api/config/payment-policy/rules with price \"50000\" (atomic units) and a config carrying only network (eip155:80002), asset, payTo, addressPatterns. Explicitly does NOT set assetTransferMethod/eip712Name/eip712Version, explaining they are derived on-chain. Mentions the facilitator requirement for a paid rule.",
      "files": [],
      "assertions": [
        "Used the route /config/payment-policy/rules (publicly /api/config/payment-policy/rules), authenticated — not an invented payment endpoint",
        "Converted 0.05 USDC at 6 decimals to the atomic-unit string \"50000\", and stated price is a non-negative integer string in atomic units",
        "Used CAIP-2 for the network: eip155:80002",
        "Included addressPatterns in the config, noting at least one is required and \".*\" opens the rule to everyone",
        "Did NOT set assetTransferMethod, eip712Name or eip712Version in the request",
        "Explained those fields are detected on-chain from the asset when the rule is saved, and why an operator-supplied EIP-712 domain would silently break settlement",
        "Mentioned that a non-zero-price rule requires a configured facilitator for that network",
        "Mentioned the node must have an RPC configured for the rule's chain"
      ]
    },
    {
      "id": 1,
      "prompt": "We want our three partner agents to call us for free but charge everyone else. And we still want to know which partner is calling. How do we set that up?",
      "expected_output": "Two rules: a zero-price rule with addressPatterns limited to the partner addresses, and a \".*\" rule at the real price. Explains cheapest-match-first evaluation, and that a zero-price rule still requires a signature — which is exactly what identifies the caller.",
      "files": [],
      "assertions": [
        "Proposed two rules: a \"0\"-price rule scoped by addressPatterns to the partners, plus a broader \".*\" rule at the paid price",
        "Explained rules are evaluated cheapest-matching-rule-first",
        "Warned (or implied correctly) that a broad rule priced below a narrow one would make the narrow one unreachable",
        "Explained a zero-price rule still makes the caller sign, and that the signature is what identifies the calling wallet — answering the 'know which partner' requirement",
        "Stated addressPatterns are regexes matched against the caller's lowercase 0x address",
        "Did not propose application-level or custom logic to implement the tiering when rules already express it"
      ]
    },
    {
      "id": 2,
      "prompt": "I added a payment rule through the console but callers say they get a 402 with an empty accepts list. The rule is definitely saved — I can see it in the rules list. What's broken?",
      "expected_output": "An empty accepts[] means no rule matched the caller — almost always addressPatterns narrower than intended. Prescribes checking the patterns against the caller's lowercase address, and verifying from the outside as a caller rather than from the operator's view of the config.",
      "files": [],
      "assertions": [
        "Diagnosed empty accepts[] as no rule matching the caller, rather than the rule being unsaved",
        "Named addressPatterns being narrower than intended as the most likely cause, and noted matching is against the caller's lowercase 0x address",
        "Prescribed verifying from the outside as a caller (a raw 402 curl, or the call-agent-a2a script) rather than trusting the operator view of the config",
        "Distinguished this symptom from the other common one — a 402 that repeats after a correctly signed retry, which points at a proxy stripping payment headers",
        "Did not suggest deleting and recreating the policy as a first remedy"
      ]
    }
  ]
}
